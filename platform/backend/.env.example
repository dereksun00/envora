# Platform database (SQLite)
DATABASE_URL="file:./dev.db"

# Shared Postgres for sandbox databases
SANDBOX_POSTGRES_HOST=localhost
SANDBOX_POSTGRES_PORT=5432
SANDBOX_POSTGRES_USER=crm
SANDBOX_POSTGRES_PASSWORD=crm_password

# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# Server
PORT=4000

# Docker
SANDBOX_NETWORK=sandbox-net
SANDBOX_POSTGRES_CONTAINER=sandbox-postgres
